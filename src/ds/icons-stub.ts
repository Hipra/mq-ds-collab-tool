/* eslint-disable */
// Null stub for @/icons — the shell uses MqIcon, not DS Icon
// DS Icon component will render nothing (acceptable; no Icon used in shell)
export const Icons: Record<string, unknown> = {};
export type IconTypes = string;
